@model KhMedium.Entities.Tag

@{
    ViewBag.Title = "Create";
    Layout = "~/Areas/admin/Views/Shared/_AdminLayoutStart.cshtml";
}

<br />
@using (Html.BeginForm("Create", "Tags", FormMethod.Post))
{
    @Html.AntiForgeryToken()
    {
        @Html.ValidationSummary(true)
        {

            <div class="row">
                <div class="col-sm-6">
                    <div class="box box-info">
                        <div class="box-header with-border">
                            <h3 class="box-title">Create Tags</h3>
                        </div>
                        <div class="box-body">
                            <div class="form-group">
                                @Html.Label("Name")
                                @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control" } })
                                @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
                            </div>
                            <div class="box-footer">
                                @Html.ActionLink("Cancel", "Index", "Tags", new { @Class = "btn btn-default" })
                                <button type="submit" class="btn btn-info pull-right">Save</button>
                            </div>
                        </div>
                    </div>
                </div>

            </div>
        }
    }
}